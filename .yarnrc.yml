# node_modules linker (NÃO PnP): exigido pela compatibilidade com Expo/Metro (apps/mobile).
nodeLinker: node-modules

# Sobe deps comuns para a raiz — Metro/React Native resolvem melhor assim.
nmHoistingLimits: none

enableGlobalCache: true
